namespace Extensions.EntityComponent
{
    /**
     * <summary>
     * Marker interface for components. Any class that implements IComponent can be added to an Entity.
     * This allows for flexible composition of behavior by attaching different components to entities.
     * </summary>
     */
    public interface IComponent
    {
        
    }
}